// Enabled for JRPG addons.
mat_motion_blur_enabled 1; mat_motion_blur_strength 0

// Since the floating health thing is broken (and technically unreadable since the game always move fast so there's no time to read it) it's automatically disabled.
tf_hud_target_id_disable_floating_health 1